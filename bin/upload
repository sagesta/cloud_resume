#!/usr/bin/env bash
set -e

# Get the directory of the script
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"

# Run the playbook using the absolute path
ansible-playbook "$PROJECT_ROOT/playbooks/upload.yml"
